export const getJetstreamCursor = (): Promise<number | undefined> => {
  // TODO
  return Promise.resolve(undefined)
}

export const saveJetstreamCursor = (cursor: number): Promise<void> => {
  // TODO
  return Promise.resolve()
}
